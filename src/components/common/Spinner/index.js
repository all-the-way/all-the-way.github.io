import React from "react";
import { IconSpinner } from "../SvgIcon";
import * as styles from "./spinner.module.css";

const Spinner = () => <IconSpinner className={styles.spinner}></IconSpinner>;

export default Spinner;
